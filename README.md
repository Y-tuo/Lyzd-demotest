# 关键指标平台

这是一个基于 React + TypeScript + Vite + TailwindCSS 构建的现代化数据指标管理平台。

## ⚠️ 当前问题

系统检测到您的环境中**未安装 Node.js**，这是运行本项目的必要条件。

## 🔧 解决方案

### 方案一：安装 Node.js（推荐）

#### macOS (使用 Homebrew)
```bash
# 1. 安装 Node.js
brew install node

# 2. 验证安装
node --version
npm --version

# 3. 进入项目目录
cd "/Volumes/zwx/Antigravity/关键指标平台"

# 4. 安装依赖
npm install

# 5. 启动开发服务器
npm run dev
```

#### macOS (使用官方安装包)
1. 访问 https://nodejs.org/
2. 下载并安装 LTS 版本
3. 重启终端后运行步骤 3-5

### 方案二：使用 nvm（Node Version Manager）
```bash
# 1. 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 2. 重启终端，然后安装 Node.js
nvm install --lts

# 3. 使用该版本
nvm use --lts

# 4-6. 同方案一的步骤 3-5
```

## 📦 项目结构

```
关键指标平台/
├── components/         # React 组件
│   ├── Header.tsx
│   ├── Sidebar.tsx
│   ├── MainContent.tsx
│   ├── IndicatorList.tsx
│   ├── IndicatorDetail.tsx
│   └── ...
├── App.tsx            # 主应用组件
├── index.tsx          # 应用入口
├── index.html         # HTML 模板
├── index.css          # 全局样式
├── package.json       # 项目依赖配置
├── tsconfig.json      # TypeScript 配置
├── vite.config.ts     # Vite 构建配置
└── tailwind.config.js # TailwindCSS 配置
```

## 🚀 可用命令

安装 Node.js 后，您可以使用以下命令：

```bash
npm run dev      # 启动开发服务器（通常在 http://localhost:5173）
npm run build    # 构建生产版本
npm run preview  # 预览生产构建
```

## 🎯 主要功能

- ✅ 指标搜索与浏览
- ✅ 指标详情查看
- ✅ 指标血缘关系可视化
- ✅ 多维度筛选
- ✅ 数据质量监控
- ✅ 安全合规管理

## 💡 提示

如果您只是想快速查看项目效果，请先安装 Node.js。整个安装过程大约需要 5-10 分钟。

## 🆘 遇到问题？

如果安装过程中遇到问题，请检查：
1. 系统是否有活动的网络连接
2. Homebrew 是否正确安装（运行 `brew --version` 检查）
3. Terminal 终端是否有足够的权限
