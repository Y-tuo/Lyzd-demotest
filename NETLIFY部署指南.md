# 部署到 Netlify 完整指南

本文档提供两种部署方法，您可以根据自己的喜好选择。

---

## 方法一：通过 Netlify 网站部署（推荐，最简单）

### 📋 准备工作

1. **确保项目可以成功构建**
   ```bash
   npm run build
   ```
   如果成功，会在项目中生成 `dist` 文件夹

2. **注册/登录 Netlify**
   - 访问：https://www.netlify.com/
   - 点击 "Sign up" 或 "Log in"
   - 推荐使用 GitHub 账号登录

### 🚀 部署步骤

#### 选项 A：拖拽上传（最快捷）

1. **构建项目**
   ```bash
   cd "/Volumes/zwx/Antigravity/关键指标平台"
   npm run build
   ```

2. **打开 Netlify Drop**
   - 访问：https://app.netlify.com/drop
   
3. **拖拽上传**
   - 将项目中的 `dist` 文件夹直接拖到页面中
   - 等待上传和部署完成（通常 10-30 秒）
   
4. **完成！**
   - Netlify 会自动生成一个 URL，例如：`https://random-name-12345.netlify.app`
   - 点击 URL 即可访问您的项目

#### 选项 B：通过 Git 部署（可持续更新）

1. **初始化 Git 仓库**（如果还没有）
   ```bash
   cd "/Volumes/zwx/Antigravity/关键指标平台"
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **推送到 GitHub**
   - 在 GitHub 创建新仓库：https://github.com/new
   - 运行命令（替换成您的仓库地址）：
   ```bash
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
   git branch -M main
   git push -u origin main
   ```

3. **在 Netlify 连接仓库**
   - 登录 Netlify：https://app.netlify.com
   - 点击 "Add new site" → "Import an existing project"
   - 选择 "GitHub"
   - 授权并选择您的仓库
   
4. **配置构建设置**（应该会自动检测到）
   - Build command: `npm run build`
   - Publish directory: `dist`
   - 点击 "Deploy site"

5. **完成！**
   - Netlify 会自动构建并部署
   - 每次推送到 GitHub，都会自动重新部署

---

## 方法二：使用 Netlify CLI（命令行）

### 📥 安装 Netlify CLI

```bash
npm install -g netlify-cli
```

### 🔐 登录 Netlify

```bash
netlify login
```
这会打开浏览器，请完成授权。

### 🚀 部署

#### 一次性部署（测试用）
```bash
cd "/Volumes/zwx/Antigravity/关键指标平台"
npm run build
netlify deploy
```
- 选择 "Create & configure a new site"
- 选择团队
- 输入站点名称（可选）
- Publish directory 输入: `dist`

#### 正式部署
```bash
netlify deploy --prod
```

### 📊 查看部署状态
```bash
netlify open
```

---

## 🎯 部署后的配置

### 自定义域名

1. 在 Netlify 控制台进入您的站点
2. 进入 "Domain settings"
3. 点击 "Add custom domain"
4. 按照提示配置 DNS 记录

### 环境变量（如果需要）

1. 在 Netlify 控制台进入您的站点
2. 进入 "Site settings" → "Environment variables"
3. 添加需要的环境变量

### HTTPS

Netlify 会自动为您的站点提供免费的 HTTPS 证书（Let's Encrypt）

---

## ✅ 验证部署

部署成功后，访问生成的 URL，检查以下功能：

- [ ] 首页正常加载
- [ ] 指标列表可以展开/折叠
- [ ] 指标详情页显示正确
- [ ] 指标空间功能正常
- [ ] 所有样式正确渲染
- [ ] 没有面包屑导航
- [ ] "指标空间"按钮正确显示

---

## 🔧 常见问题

### Q: 部署后页面空白
A: 检查浏览器控制台是否有错误，通常是路径问题

### Q: 刷新页面出现 404
A: 确保 `netlify.toml` 文件中有正确的重定向规则（已包含）

### Q: 资源加载失败
A: 检查 `vite.config.ts` 中的 `base` 配置

### Q: 构建失败
A: 在本地先运行 `npm run build` 检查是否有错误

---

## 📱 推荐配置

### 自动部署预览

在 Netlify 设置中启用：
- Branch deploys（分支部署）
- Deploy previews（PR 预览）

这样每次推送代码都会有预览链接。

### 性能优化

Netlify 已经自动启用了：
- CDN 加速
- 资源压缩
- Brotli 压缩
- HTTP/2

---

## 🎉 快速开始（最快方法）

如果您只想快速看到效果：

```bash
# 1. 构建项目
cd "/Volumes/zwx/Antigravity/关键指标平台"
npm run build

# 2. 打开 dist 文件夹
open dist

# 3. 访问 https://app.netlify.com/drop

# 4. 把整个 dist 文件夹拖进去

# 5. 完成！
```

总共只需要 1 分钟！

---

需要帮助？访问 Netlify 文档：https://docs.netlify.com/
